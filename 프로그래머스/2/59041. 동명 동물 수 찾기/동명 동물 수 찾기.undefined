-- 코드를 입력하세요
SELECT NAME, COUNT(*) as count
from animal_ins
where name is not null
group by NAME
having count(*)>=2
order by name

# SELECT name, count(*) as count
# from animal_ins
# where name is not null
# group by name
# having count(*)>=2
# order by name